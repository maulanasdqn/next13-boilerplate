import { NextPage } from "next";
import { ReactElement } from "react";

const DashboardLoading: NextPage = (): ReactElement => {
  return <span>Loading...</span>;
};

export default DashboardLoading;
