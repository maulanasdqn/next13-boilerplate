"use client";
import { FC, ReactElement } from "react";

export const DashboardUserPermissionModule: FC = (): ReactElement => {
  return (
    <section className="flex w-full h-full">
      <h1>User Permission Module</h1>
    </section>
  );
};
