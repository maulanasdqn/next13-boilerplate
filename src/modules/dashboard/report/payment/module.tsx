"use client";

import { FC, ReactElement } from "react";

export const DashboardReportPaymentModule: FC = (): ReactElement => {
  return (
    <section className="flex w-full h-full">
      <h1>Dashboard</h1>
    </section>
  );
};
